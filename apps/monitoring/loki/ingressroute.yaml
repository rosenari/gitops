apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: loki
  namespace: monitoring
spec:
  entryPoints:
    - loki
  routes:
    - match: Host(`*`) || PathPrefix(`/`)
      kind: Rule
      services:
        - name: loki
          port: 3100