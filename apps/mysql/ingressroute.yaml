apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: mysql
  namespace: mysql
spec:
  entryPoints:
    - mysql
  routes:
    - match: HostSNI(`*`)
      services:
        - name: mysql
          port: 3306